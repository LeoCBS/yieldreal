version: "3.9"

services:
  frontend:
    image: node:20
    container_name: yieldreal-frontend-dev
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "5173:5173"
    command: sh -c "npm install && npm run dev -- --host"
